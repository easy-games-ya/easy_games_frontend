export enum PlayingCardType {
  QUESTION,
  CATEGORY,
};

export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
  LINK = 'link',
};

export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  EMAIL = 'email',
};
